node_modules
.next
out
.env*.local
*.log
.firebase
**/.firebase
*.tsbuildinfo

# Local CRM data backups — contain customer PII, never commit (repo is public)
backups/
*.crm-backup.json

# Unrelated local working files (kept on disk, out of this repo)
/Veggie Cart - Phase 2 App Requirement Document.md
/scripts/
