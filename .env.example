# Self-hosted sync endpoint. Served same-origin as the app (crm docroot /api.php).
NEXT_PUBLIC_SYNC_API_URL=/api.php
# Shared bearer token the endpoint requires. Must match crm-sync-config.php on the server.
NEXT_PUBLIC_SYNC_TOKEN=replace-with-a-long-random-secret
